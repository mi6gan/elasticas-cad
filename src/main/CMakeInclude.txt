file(GLOB MAIN_CPPS src/main/*.cpp) 
file(GLOB MAIN_DIALOGS_CPPS src/main/dialogs/*.cpp)
set(MAIN_CPPS ${MAIN_CPPS} ${MAIN_DIALOGS_CPPS})
set(MAIN_LINK_LIBS elcore ${QT_QTSQL_LIBRARY} ${PYTHON_LIBRARIES})
if(WIN32)
	if(MSVC)
	    add_definitions(-D_USE_MATH_DEFINES)
	else()
	    set (MAIN_LINK_LIBS ${MAIN_LINK_LIBS} mingw32)
		add_definitions(-Dmain=qMain)
	endif()
	set (MAIN_LINK_LIBS ${MAIN_LINK_LIBS} ${QT_QTMAIN_LIBRARY} )
endif()
add_executable(elasticas ${MAIN_CPPS})
target_link_libraries(elasticas ${MAIN_LINK_LIBS})
set_target_properties(elasticas PROPERTIES RUNTIME_OUTPUT_DIRECTORY ${ELOUTPUT_DIR} AUTOMOC TRUE)
